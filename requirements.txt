# Pinned to minor versions: Streamlit Community Cloud resolves these at build
# time and an unpinned scikit-learn will happily unpickle a model it cannot
# actually run.
streamlit>=1.50,<2.0
pandas>=2.1,<3.1
numpy>=1.26,<3.0
scikit-learn>=1.4,<2.0
plotly>=5.20,<7.0
joblib>=1.3,<2.0
xlrd>=2.0,<3.0          # legacy .xls container; openpyxl cannot read it
shapely>=2.0,<3.0       # boundary simplification, scripts/01_prepare_geo.py only
